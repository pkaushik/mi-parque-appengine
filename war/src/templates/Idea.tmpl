<div data-role="collapsible" {{if first}} data-collapsed="false" {{else}} data-collapsed="true"{{/if}} data-theme="d" >
  <h3>${title}</h3>
  <div class="ui-body ui-body-c">
  <p>
    ${description}
  </p>
  <fb:like href="./pages/ideas/${id}.html"
	   layout="button_count"
	   show_faces="false"
	   action="like"
	   colorscheme="light"></fb:like>
  </div>
</div>